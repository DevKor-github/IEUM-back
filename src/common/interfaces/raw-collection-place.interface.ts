export interface RawCollectionPlace {
  placeId: number;
  kakaoId: string;
  placeName: string;
  address: string; //제주 서귀포시
  primaryCategory: string; //Cafe .etc
  placeKeyword: string;
  isSaved: boolean;
}
